{
  "name": "@depot/workers",
  "version": "1.0.0",
  "description": "Cloudflare Worker handlers for depot",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "dev": "tsc -b --watch",
    "test": "vitest run",
    "format": "prettier --write \"src/**/*.{js,ts,tsx}\"",
    "lint": "tsc -b && prettier --check \"src/**/*.{js,ts,tsx}\"",
    "typecheck": "tsc -b"
  },
  "author": "Francois Laubscher",
  "license": "MIT",
  "dependencies": {
    "@depot/core": "workspace:*",
    "openai": "^6.44.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "prettier": "^3.8.4",
    "rimraf": "^5.0.10",
    "typescript": "^5.9.3",
    "vitest": "^3.2.6"
  }
}
